all:
  hosts:
    toweringstoat
  children:
    apt_repositories:
      hosts:
        toweringstoat
    apt_packages:
      hosts:
        toweringstoat
    nodejs_gpg:
      hosts:
        toweringstoat
    asdf:
      hosts:
        toweringstoat
    keeagent:
      hosts:
        toweringstoat
